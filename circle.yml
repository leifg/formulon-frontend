machine:
  node:
    version: 5.8.0

dependencies:
  pre:
    - NODE_ENV=development npm prune
    - NODE_ENV=development npm install

test:
  override:
    - npm run test
  post:
    - npm run semantic-release || true
